# Appendix D：缺口修补与 fresh RT 验证

本次完整执行：[结果目录](result/appendix_d_20260921_235448_775259/)。
[运行 manifest](result/appendix_d_20260921_235448_775259/manifest.json) 记录 E4–E8、fresh RT 生成与静态评估全部完成。
工作 notebook 已保存本次执行输出，仍全部为英文。原 `Nulling_CDF_SectorDrop.ipynb` 的 SHA256 在运行前后相同。

## 为什么此前没有 RT

旧 Appendix D 入口只实现了声明有限模型中的 SOCP/LP 数值核对，以及可选的旧静态缓存评估，没有连接原工程的场景生成部分。
这可以验证数学实现，但不能替代用户所需的重新 drop UE、ray tracing 和物理场景验证。

现在新增 `appendix_d_experiments/rt_source.py`，直接复用原工程的场景、位置验收、DL/UL RT、相干信道处理和 MUSIC。
每个 macro 都重新抽样 TN/NTN 位置与卫星方向，重新追踪；没有把旧缓存作为新 RT 使用。
同一 seed 可复现同一批随机抽样；改变 seed 才得到另一批随机实现。

独立静态 drop 与动态轨迹仍需区分：动态实验应先 drop 一次，再在同一 episode 中演化用户位置、活动、监听和延迟。
逐 tick 独立重 drop 会改变问题，无法自动产生有意义的信息年龄与重复观测相关性。
当前 fresh RT 验证的是静态空间保护；E7/E8 的动态控制与新增诊断仍使用显式声明的合成模型。

## 修补内容

- **真实重复观测：** 新增固定两次监听对照；独立与时间相关观测在相同物理轨迹上执行，输出实际监听次数、重复比例、接受事件相关性及配对区间。原 J burst 情景会明确标记是否实际重复监听。
- **随机未知到达：** 到达时间不进入控制器接口；比较先验背景保护、无响应、检测结果释放后启用保护。分别运行 UL 活跃与永久 UL 静默接收机，保存发现、处理释放、保护启用和分阶段暴露。
- **校准目标：** E4 的 `alpha` 默认0.10，quick/full不再改变它；需要0.05时显式指定。
- **单位：** 动态汇总明确为整条 episode 的累计归一化 bits/Hz。
- **fresh RT 入口：** notebook 默认 `SPATIAL_SOURCE='fresh_rt'`，可选择 `cache` 或 `none`；CLI 提供 `--fresh-rt`、`--spatial-only`、`--rt-macros`、`--rt-max-depth`。

新增机制诊断使用固定因果策略，**不是随机到达联合问题的最优 J 策略**。此次固定诊断的最差 TN 窗口失败率为100%，已在CSV中保留，不能拿这些结果声称满足原 TN QoS 的联合服务收益。

## 新诊断的实际结果

重复监听两组每条轨迹均监听2次，重复率100%。接受事件相关系数：独立组 −0.029，95% bootstrap区间[−0.136,0.074]；相关组为1。
前景超标比例分别7.458%与7.375%；配对超标差区间跨零。因此可以确认时间相关性被真正施加，但此次参数下没有证据证明它显著恶化性能。

随机到达诊断每种情景400条轨迹，共2855个接收机DL活动tick，各比较器共享到达时刻、检测随机数和监听开销：

| 接收机 | 先验保护超标比例 | 无响应 | 延迟检测后保护 | 检出率 |
|---|---:|---:|---:|---:|
| UL活跃新到达 | 0% | 47.531% | 24.448% | 67.5% |
| 永久UL静默新到达 | 0% | 47.531% | 47.531% | 0% |

UL活跃接收机在延迟策略的保护启用前超标44.487%，启用后观测超标0。零观测不等于统计证明风险为零；结果同时提供完整episode区间与保守上界。
永久UL静默者不能靠被动UL监听被发现；没有先验包络时，延迟发现方案不提供完整活动时域的保证。

证据：[重复观测](result/appendix_d_20260921_235448_775259/E8/repeated_observation/summary.json)、[随机到达](result/appendix_d_20260921_235448_775259/E8/random_arrival/summary.csv)。

## 真实 Sionna RT 的实际结果

本次6个独立macro，训练/校准/测试为2/2/2。逐文件核实6组 TN 位置和6组 NTN 位置均不同。
每个macro包含12个TN、20个NTN、12个sector、64阵元，DL 7 GHz、UL 6.3 GHz，RT深度3；有限800快拍MDL-MUSIC。
quick使用每源10,000条射线与2度搜索网格；full增加为100,000条射线与0.5度网格，因此物理近似精度也改变，正式比较时应冻结这些设置。
UL感知噪声以23 dBm UL功率归一化，与旧 notebook 的35 dBm参考归一化不同；不能把两者的感知结果当作同一设置。

测试覆盖全部24个sector，共480个NTN-sector配对，而不是只截取第一个测试macro。

| Γ (dB) | TN达到−6 dB门限的比例 | 中位发射功率/Pmax | TN中位SNR (dB) | NTN INR 95%分位 (dB) |
|---:|---:|---:|---:|---:|
| 无保护 | 100% | 1 | 29.743 | −9.817 |
| −15 | 58.33% | 0.000320 | −5.214 | −47.357 |
| −10 | 83.33% | 0.001012 | −0.214 | −42.357 |
| −5 | 91.67% | 0.003199 | 4.786 | −37.361 |
| 0 | 100% | 0.010116 | 9.786 | −32.359 |

所有受约束点在这两个测试macro中均未观测到超标，但不能据此做尾部风险认证。
当前跨场景统一的经验集合很保守（C≈64.97，ρ≈9.94），大量保护来自降功率，而非单纯空间零陷。
Γ=−15 dB时中位功率只剩0.032% Pmax，10/24个sector未达到TN门限，因此低INR必须与服务损失一起解释。
下一步物理建模的重点是用独立训练/校准数据细化可观测条件下的保护集合，并检查跨频漏检与残差；不能按测试真值缩小集合以获得好看的图。

证据：[真实RT汇总](result/appendix_d_20260921_235448_775259/fresh_rt_spatial/summary.json)、[Gamma统计](result/appendix_d_20260921_235448_775259/fresh_rt_spatial/gamma_summary.csv)。

## 验证记录

- 34项 Appendix D 回归测试全部通过，含旧因果性/LP测试、新校准目标、fresh RT来源隔离/失败留痕、入口路由和新机制诊断。
- 在现有 `sionna20` kernel 中完整执行18个notebook单元中的10个代码单元，成功保存全部图表与结果。
- Notebook通过nbformat结构验证，无中文残留；当前实验源码与本次归档SHA256一致（执行后notebook新增输出除外）。
- 六个真实drop均完成DL/UL追踪及感知，旧SectorDrop notebook运行前后哈希一致。

运行示例：

```bash
python run_appendix_d.py --profile quick --fresh-rt
python run_appendix_d.py --profile quick --fresh-rt --spatial-only --rt-macros 6
```
